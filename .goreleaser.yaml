version: 2
project_name: braintrust-x-go

builds:
  - skip: true

release:
  github:
    owner: braintrustdata
    name: braintrust-x-go
  draft: false
  prerelease: auto
  footer: |
    ## Thanks
    Thanks to everyone who contributed to {{ .Tag }}.

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - '^ci:'
      - 'merge pull request'
