version: "2"
linters:
  default: standard
  enable:
    - bodyclose
    - usetesting
    #- thelper

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/braintrust/braintrust-x-go
